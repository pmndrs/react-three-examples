dist/
node_modules/
reference/
test-results/
screenshots/
pnpm-lock.yaml

# Generated research data dumps — large, machine-written, no value in reformatting.
research/data/

# Dennis's personal notes; his formatting, not the formatter's.
styleNotes.md

# Deliberately non-canonical scratch (see the banner in the file).
docs/style-drafts/

# Generated by scripts/generate-manifest.mjs, which owns its formatting.
# Prettier collapses short arrays at 120 cols; the generator always expands. Letting
# both format it makes `pnpm lint` and `generate:manifest --check` disagree forever.
src/examples.json
